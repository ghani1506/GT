# Gulintangan Brunei Streamlit App

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```

For Streamlit Cloud, upload `app.py` and `requirements.txt` to your GitHub repository.

Optional: add a gong image at `assets/gong.jpg`. The app works even if that image is missing.
